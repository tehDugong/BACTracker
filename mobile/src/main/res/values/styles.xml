<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar"/>

    <style name="PhoneTheme" parent ="Theme.AppCompat.Light">
        <item name="android:actionBarStyle">@style/PhoneActionBar</item>
        <item name="actionBarStyle">@style/PhoneActionBar</item>

    </style>

    <style name="PhoneActionBar" parent="Widget.AppCompat.ActionBar">
        <item name="titleTextStyle">@style/ActionBarText</item>
        <item name="android:background">@color/colorRed</item>
        <item name="background">@color/colorRed</item>

    </style>

    <style name="ActionBarText" parent="TextAppearance.AppCompat.Widget.ActionBar.Title">
        <item name="android:textColor">@color/brightYellow</item>
    </style>


        <!-- Customize your theme here. -->


</resources>
