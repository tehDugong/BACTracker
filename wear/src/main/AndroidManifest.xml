<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.cs160.bactracker" >

    <uses-feature android:name="android.hardware.type.watch" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@android:style/Theme.DeviceDefault" >
        <activity
            android:name=".WatchActivity"
            android:label="@string/app_name" >
        </activity>
        <activity
            android:name=".BACActivity"
            android:label="@string/title_activity_bac" >
        </activity>

        <service
            android:name=".WatchListeningService"
            android:enabled="true"
            android:exported="true" >
        </service>

        <activity
            android:name=".StartDrinkingActivity"
            android:label="@string/title_activity_start_drinking" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".DrinksListActivity"
            android:label="@string/title_activity_drinks_list" >
        </activity>
        <activity
            android:name=".CountDrinks"
            android:label="@string/title_activity_count_drinks" >
        </activity>
    </application>

</manifest>
